<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: linear-gradient(135deg, #3498DB 0%, #2980B9 100%); color: white; padding: 30px; text-align: center; border-radius: 8px 8px 0 0; }
    .content { background: #fff; padding: 30px; border: 1px solid #e0e0e0; }
    .tracking-box { background: #FFF3CD; padding: 20px; border-radius: 8px; margin: 20px 0; border-left: 4px solid #FFC107; }
    .tracking-number { font-size: 20px; font-weight: bold; color: #333; font-family: monospace; letter-spacing: 2px; }
    .button { display: inline-block; background: #3498DB; color: white; padding: 12px 30px; text-decoration: none; border-radius: 25px; margin: 20px 0; }
    .footer { text-align: center; padding: 20px; color: #999; font-size: 12px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>📦 Votre colis est en route !</h1>
    </div>
    
    <div class="content">
      <p>Bonjour <strong><%= @buyer.email.split('@').first.capitalize %></strong>,</p>
      
      <p>Bonne nouvelle ! Le vendeur vient d'expédier votre commande.</p>
      
      <p><strong>Produit :</strong> <%= @product.name %></p>
      <p><strong>Numéro de commande :</strong> #<%= @order.id %></p>
      <p><strong>Date d'expédition :</strong> <%= @order.shipped_at.strftime("%d/%m/%Y à %H:%M") %></p>
      
      <% if @order.tracking_number.present? %>
        <div class="tracking-box">
          <p><strong>📍 Numéro de suivi :</strong></p>
          <p class="tracking-number"><%= @order.tracking_number %></p>
          <p><small>Conservez ce numéro pour suivre votre colis auprès du transporteur.</small></p>
        </div>
      <% end %>
      
      <p><strong>Adresse de livraison :</strong><br>
      <%= @order.shipping_address %>
      </p>
      
      <p>Votre colis devrait arriver dans les prochains jours selon le mode de livraison choisi (<%= @order.shipping_method_name %>).</p>
      
      <center>
        <a href="<%= order_url(@order) %>" class="button">Suivre ma commande</a>
      </center>
    </div>
    
    <div class="footer">
      <p>MarketPlace - Plateforme de vente en ligne<br>
      Cet email a été envoyé automatiquement, merci de ne pas y répondre.</p>
    </div>
  </div>
</body>
</html>
