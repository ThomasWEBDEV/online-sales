<div class="container my-5 text-center">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card shadow-lg">
        <div class="card-body p-5">
          <div class="mb-4">
            <i class="fas fa-check-circle text-success" style="font-size: 80px;"></i>
          </div>

          <h1 class="mb-3">Paiement réussi !</h1>
          <p class="lead mb-4">Merci pour votre achat. Votre commande a été confirmée.</p>

          <% if @order %>
            <div class="alert alert-info">
              <strong>Numéro de commande : #<%= @order.id %></strong>
            </div>

            <p class="text-muted mb-4">
              Un email de confirmation vous a été envoyé à <strong><%= current_user.email %></strong>
            </p>

            <div class="d-grid gap-2">
              <%= link_to "Voir ma commande", order_path(@order), class: "btn btn-flat btn-lg" %>
              <%= link_to "Mes achats", my_purchases_path, class: "btn btn-outline-primary btn-lg" %>
              <%= link_to "Retour à l'accueil", root_path, class: "btn btn-ghost btn-lg" %>
            </div>
          <% else %>
            <p class="text-muted">Le statut de votre commande sera mis à jour dans quelques instants.</p>
            <%= link_to "Retour à l'accueil", root_path, class: "btn btn-flat btn-lg mt-3" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
