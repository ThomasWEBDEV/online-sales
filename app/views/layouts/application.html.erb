<!DOCTYPE html>
<html>
  <head>
    <title>MarketPlace - Plateforme de vente en ligne</title>

    <!-- Favicons professionnels -->
    <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🛒</text></svg>">
    <link rel="apple-touch-icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🛒</text></svg>">

    <meta property="og:title" content="MarketPlace - Plateforme de vente en ligne" />
    <meta property="og:description" content="Achetez et vendez en toute simplicité sur notre marketplace moderne et sécurisée." />
    <meta property="og:type" content="website" />
    <meta property="og:image" content="https://via.placeholder.com/1200x630/667eea/ffffff?text=MarketPlace" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />

    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap 5 CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <%= javascript_importmap_tags %>
    <!-- NOS STYLES EN DERNIER POUR ÉCRASER BOOTSTRAP -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <!-- Navbar améliorée -->
    <nav class="navbar navbar-expand-lg navbar-lewagon sticky-top">
      <div class="container-fluid">
        <%= link_to root_path, class: "navbar-brand d-flex align-items-center" do %>
          <div class="brand-icon">
            <i class="fas fa-store"></i>
          </div>
          <span class="brand-text">MarketPlace</span>
        <% end %>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <% if user_signed_in? %>
              <li class="nav-item">
                <%= link_to new_product_path, class: "nav-link btn-sell me-2" do %>
                  <i class="fas fa-plus-circle me-1"></i>Vendre
                <% end %>
              </li>

              <!-- LIEN FAVORIS AJOUTÉ -->
              <li class="nav-item">
                <%= link_to favorites_path, class: "nav-link position-relative me-2" do %>
                  <i class="fas fa-heart me-1"></i>Favoris
                  <% if current_user.favorites.count > 0 %>
                    <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                      <%= current_user.favorites.count %>
                    </span>
                  <% end %>
                <% end %>
              </li>

              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle user-menu" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  <i class="fas fa-user-circle me-1"></i>Mon compte
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to "Mes produits", products_path, class: "dropdown-item" %></li>
                  <li><hr class="dropdown-divider"></li>
                  <li>
                    <%= button_to "Déconnexion", destroy_user_session_path,
                        method: :delete,
                        form: { data: { turbo: false } },
                        class: "dropdown-item btn-link p-0 border-0 text-start w-100" %>
                  </li>
                </ul>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "Connexion", new_user_session_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Inscription", new_user_registration_path, class: "nav-link btn-outline-primary ms-2" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Messages flash stylisés -->
    <% if notice %>
      <div class="alert alert-flash alert-success" role="alert">
        <div class="alert-content">
          <i class="fas fa-check-circle"></i>
          <div class="alert-text"><%= notice %></div>
        </div>
      </div>
    <% end %>

    <% if alert %>
      <div class="alert alert-flash alert-danger" role="alert">
        <div class="alert-content">
          <i class="fas fa-exclamation-triangle"></i>
          <div class="alert-text"><%= alert %></div>
        </div>
      </div>
    <% end %>

    <!-- Contenu principal des pages -->
    <main>
      <div class="container mt-4 mb-5">
        <%= yield %>
      </div>
    </main>

    <!-- Footer professionnel -->
    <footer class="footer-normal">
      <div class="footer-content">
        <div class="footer-grid">
          <!-- Section À propos -->
          <div class="footer-section footer-about">
            <div class="brand-logo">
              <i class="fas fa-store"></i>
              <span>MarketPlace</span>
            </div>
            <p>La marketplace de confiance pour acheter et vendre en toute sécurité. Des milliers de produits et une communauté de vendeurs vérifiés.</p>
            <div class="stats-mini">
              <div class="stat">
                <span class="number"><%= Product.count %></span>
                <span class="label">Produits</span>
              </div>
              <div class="stat">
                <span class="number"><%= User.count %></span>
                <span class="label">Membres</span>
              </div>
              <div class="stat">
                <span class="number"><%= Product.where(sold: true).count %></span>
                <span class="label">Ventes</span>
              </div>
            </div>
          </div>

          <!-- Navigation -->
          <div class="footer-section">
            <h4>Navigation</h4>
            <ul>
              <li><%= link_to root_path do %><i class="fas fa-home"></i>Accueil<% end %></li>
              <li><%= link_to products_path do %><i class="fas fa-shopping-bag"></i>Produits<% end %></li>
              <% if user_signed_in? %>
                <li><%= link_to new_product_path do %><i class="fas fa-plus"></i>Vendre<% end %></li>
                <li><%= link_to favorites_path do %><i class="fas fa-heart"></i>Mes favoris<% end %></li>
              <% else %>
                <li><%= link_to new_user_registration_path do %><i class="fas fa-user-plus"></i>Inscription<% end %></li>
                <li><%= link_to new_user_session_path do %><i class="fas fa-sign-in-alt"></i>Connexion<% end %></li>
              <% end %>
            </ul>
          </div>

          <!-- Support -->
          <div class="footer-section">
            <h4>Support</h4>
            <ul>
              <li><a href="#"><i class="fas fa-question-circle"></i>Centre d'aide</a></li>
              <li><a href="#"><i class="fas fa-envelope"></i>Contact</a></li>
              <li><a href="#"><i class="fas fa-shield-alt"></i>Sécurité</a></li>
              <li><a href="#"><i class="fas fa-truck"></i>Livraison</a></li>
              <li><a href="#"><i class="fas fa-undo"></i>Retours</a></li>
            </ul>
          </div>

          <!-- Informations légales et réseaux -->
          <div class="footer-section footer-social">
            <h4>Nous suivre</h4>
            <div class="social-links">
              <a href="#" title="Facebook"><i class="fab fa-facebook-f"></i></a>
              <a href="#" title="Twitter"><i class="fab fa-twitter"></i></a>
              <a href="#" title="Instagram"><i class="fab fa-instagram"></i></a>
              <a href="#" title="LinkedIn"><i class="fab fa-linkedin-in"></i></a>
            </div>
            <ul style="margin-top: 20px;">
              <li><a href="#"><i class="fas fa-file-contract"></i>CGV</a></li>
              <li><a href="#"><i class="fas fa-user-shield"></i>Confidentialité</a></li>
              <li><a href="#"><i class="fas fa-cookie-bite"></i>Cookies</a></li>
            </ul>
          </div>
        </div>

        <div class="footer-bottom">
          <div class="footer-bottom-content">
            <div class="copyright">
              &copy; 2024 MarketPlace - Tous droits réservés
            </div>
            <div class="footer-links">
              <a href="#">Mentions légales</a>
              <a href="#">Plan du site</a>
              <a href="#">Accessibilité</a>
            </div>
          </div>
        </div>
      </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Fix Bootstrap dropdowns avec Turbo -->
    <script>
      document.addEventListener('turbo:load', function() {
        // Réinitialiser tous les dropdowns Bootstrap après navigation Turbo
        var dropdownElementList = [].slice.call(document.querySelectorAll('[data-bs-toggle="dropdown"]'))
        dropdownElementList.map(function (dropdownToggleEl) {
          return new bootstrap.Dropdown(dropdownToggleEl)
        })
      })
    </script>
  </body>
</html>
