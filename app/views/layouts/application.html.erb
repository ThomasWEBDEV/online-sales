<!DOCTYPE html>
<html>
  <head>
    <title>MarketPlace - Plateforme de vente en ligne</title>

    <!-- Favicons professionnels -->
    <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🛒</text></svg>">
    <link rel="apple-touch-icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🛒</text></svg>">

    <meta property="og:title" content="MarketPlace - Plateforme de vente en ligne" />
    <meta property="og:description" content="Achetez et vendez en toute simplicité sur notre marketplace moderne et sécurisée." />
    <meta property="og:type" content="website" />
    <meta property="og:image" content="https://via.placeholder.com/1200x630/667eea/ffffff?text=MarketPlace" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />

    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap 5 CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <%= javascript_importmap_tags %>
    <!-- NOS STYLES EN DERNIER POUR ÉCRASER BOOTSTRAP -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <!-- Navbar améliorée -->
    <nav class="navbar navbar-expand-lg navbar-lewagon sticky-top">
      <div class="container-fluid">
        <%= link_to root_path, class: "navbar-brand d-flex align-items-center" do %>
          <div class="brand-icon">
            <i class="fas fa-store"></i>
          </div>
          <span class="brand-text">MarketPlace</span>
        <% end %>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <% if user_signed_in? %>
              <li class="nav-item">
                <%= link_to new_product_path, class: "nav-link btn-sell me-2" do %>
                  <i class="fas fa-plus-circle me-1"></i>Vendre
                <% end %>
              </li>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle user-menu" href="#" role="button" data-bs-toggle="dropdown">
                  <i class="fas fa-user-circle me-1"></i>Mon compte
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to "Mes produits", products_path, class: "dropdown-item" %></li>
                  <li><hr class="dropdown-divider"></li>
                  <li>
                    <%= button_to "Déconnexion", destroy_user_session_path,
                        method: :delete,
                        form: { data: { turbo: false } },
                        class: "dropdown-item btn-link p-0 border-0 text-start w-100" %>
                  </li>
                </ul>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "Connexion", new_user_session_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Inscription", new_user_registration_path, class: "nav-link btn-outline-primary ms-2" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Messages flash stylisés -->
    <% if notice %>
      <div class="alert alert-flash alert-success" role="alert">
        <div class="alert-content">
          <i class="fas fa-check-circle"></i>
          <div class="alert-text"><%= notice %></div>
        </div>
      </div>
    <% end %>

    <% if alert %>
      <div class="alert alert-flash alert-danger" role="alert">
        <div class="alert-content">
          <i class="fas fa-exclamation-triangle"></i>
          <div class="alert-text"><%= alert %></div>
        </div>
      </div>
    <% end %>

    <!-- Contenu principal des pages -->
    <main>
      <div class="container mt-4 mb-5">
        <%= yield %>
      </div>
    </main>

    <!-- Footer avec la bonne classe -->
    <footer class="footer-normal">
      <div class="container">
        <p class="mb-0">&copy; 2024 MarketPlace - Plateforme de vente en ligne</p>
      </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
